console.log('I am in ');

const Monday = ['Christmas', 'Thanksgiving', 'Time', 'Water'];

const Wings = (name) => {
  for (i = 0; i < name.length; i++) {
    console.log(name[i]);
  }
};
Wings(Monday);
